[tox]
envlist = py38, py39, unused
skipsdist = True

[gh-actions]
python =
    3.8: py38
    3.9: py39

[testenv]
allowlist_externals =
    bash
    mkdir
commands =
    mkdir -p out
    bash -c 'touch out/$TOX_ENV_NAME'
